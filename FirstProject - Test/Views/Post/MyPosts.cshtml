@using FirstProjectRepository.DBModels;
@model List<Post>

<h2>My Posts</h2>

@if (Model.Count == 0)
{
    <p>No posts found.</p>
}
else
{
    <ul>
        @foreach (var post in Model)
        {
            <li>@post.title</li>
        }
    </ul>
}